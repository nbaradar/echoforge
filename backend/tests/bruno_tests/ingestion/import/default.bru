meta {
  name: default
  type: http
  seq: 1
}

post {
  url: {{host}}/api/import
  body: json
  auth: inherit
}

body:json {
  {
    "memories": [
      {
        "title": "Title",
        "content": "memory data"
      }
    ]
  }
}
